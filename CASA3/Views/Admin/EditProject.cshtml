@model Core.DTOs.ProjectCreateDto
@{
    ViewData["Title"] = "Edit Project";
    var projectId = ViewData["ProjectId"] as string;
}

<section class="p-8 max-w-2xl">
    <div class="mb-8 flex flex-wrap items-center gap-4">
        <a asp-controller="Admin" asp-action="Projects" class="text-[#1E403F] hover:underline">&larr; Back to Projects</a>
        @if (!string.IsNullOrEmpty(projectId))
        {
            <a asp-controller="Admin" asp-action="ProjectUnits" asp-route-projectId="@projectId" class="text-[#b39359] hover:underline font-medium">Manage Units</a>
        }
    </div>
    <h1 class="text-2xl font-bold text-gray-800 mb-6">Edit Project</h1>

    <form asp-controller="Admin" asp-action="EditProject" asp-route-id="@projectId" method="post" enctype="multipart/form-data" class="space-y-5">
        <div asp-validation-summary="All" class="text-red-600 text-sm"></div>

        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Project name <span class="text-red-500">*</span></label>
            <input asp-for="Name" type="text" class="w-full border-2 border-gray-200 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#b39359] focus:border-[#b39359]" placeholder="e.g. CAPRI ISLAND" />
            <span asp-validation-for="Name" class="text-red-600 text-sm"></span>
        </div>

        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">URL (optional)</label>
            <input asp-for="Url" type="text" class="w-full border-2 border-gray-200 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#b39359] focus:border-[#b39359]" placeholder="e.g. /projects/capri" />
        </div>

        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Slug (optional)</label>
            <input asp-for="Slug" type="text" class="w-full border-2 border-gray-200 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#b39359] focus:border-[#b39359]" placeholder="e.g. capri-island" />
        </div>

        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Description (optional)</label>
            <textarea asp-for="Description" rows="4" class="w-full border-2 border-gray-200 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#b39359] focus:border-[#b39359]" placeholder="Main project description..."></textarea>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-5">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Year</label>
                <input asp-for="Year" type="number" class="w-full border-2 border-gray-200 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#b39359] focus:border-[#b39359]" placeholder="e.g. 2025" />
                <p class="text-gray-500 text-xs mt-1">Year the project was started or completed.</p>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Category</label>
                <select asp-for="Category" class="w-full border-2 border-gray-200 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#b39359] focus:border-[#b39359]">
                    <option value="">— Select —</option>
                    <option value="@((int)Core.Enum.ProjectCategory.Luxury)">Luxury</option>
                    <option value="@((int)Core.Enum.ProjectCategory.Premium)">Premium</option>
                    <option value="@((int)Core.Enum.ProjectCategory.MidPremium)">Mid-Premium</option>
                </select>
            </div>
            <div class="flex items-center pt-6">
                <label class="relative inline-flex items-center cursor-pointer">
                    <input asp-for="IsFeatured" type="checkbox" value="true" class="sr-only peer" />
                    <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-2 peer-focus:ring-[#b39359] rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-[#1E403F]"></div>
                    <span class="ml-3 text-sm font-medium text-gray-700">Featured Project</span>
                </label>
            </div>
        </div>

        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Hero image</label>
            <input asp-for="HeroImageUrl" type="text" class="w-full border-2 border-gray-200 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#b39359] focus:border-[#b39359]" placeholder="Or paste URL (optional)" />
            <p class="text-gray-500 text-xs mt-1">Or upload to replace: JPG, PNG, GIF, WebP (max 15MB)</p>
            <input type="file" name="heroImageFile" accept=".jpg,.jpeg,.png,.gif,.webp" class="mt-2 block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:bg-[#1E403F] file:text-white file:font-medium hover:file:bg-[#163332]" />
        </div>

        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Brochure PDF</label>
            <input asp-for="BrochurePdfUrl" type="text" class="w-full border-2 border-gray-200 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-[#b39359] focus:border-[#b39359]" placeholder="Or paste URL (optional)" />
            <p class="text-gray-500 text-xs mt-1">Or upload to replace: PDF (max 15MB)</p>
            <input type="file" name="brochurePdfFile" accept=".pdf" class="mt-2 block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:bg-[#1E403F] file:text-white file:font-medium hover:file:bg-[#163332]" />
        </div>

        <div class="flex gap-3 pt-2">
            <button type="submit" class="bg-[#1E403F] text-white px-6 py-3 rounded-lg font-semibold hover:bg-[#163332] transition-colors">Save changes</button>
            <a asp-controller="Admin" asp-action="Projects" class="bg-gray-200 text-gray-800 px-6 py-3 rounded-lg hover:bg-gray-300 transition-colors">Cancel</a>
        </div>
    </form>
</section>

@if (!string.IsNullOrEmpty(projectId))
{
<!-- Gallery Images -->
<section class="p-8 max-w-4xl mt-4 border-t border-gray-200">
    <h2 class="text-xl font-bold text-gray-800 mb-2">Gallery Images</h2>
    <p class="text-sm text-gray-500 mb-6">These appear as a photo gallery on the public project detail page, below the Download Brochure section.</p>

    <!-- Upload form -->
    <div class="bg-gray-50 border-2 border-dashed border-gray-300 rounded-xl p-6 mb-6">
        <h3 class="text-sm font-semibold text-gray-700 mb-3">Add New Image</h3>
        <div class="flex flex-col sm:flex-row gap-3">
            <input type="file" id="galleryFileInput" accept=".jpg,.jpeg,.png,.gif,.webp"
                   class="block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:bg-[#1E403F] file:text-white file:font-medium hover:file:bg-[#163332]" />
            <input type="text" id="galleryCaptionInput" placeholder="Caption (optional)"
                   class="border-2 border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-[#b39359] focus:border-[#b39359] w-full sm:w-64" />
            <button onclick="uploadGalleryImage()" id="galleryUploadBtn"
                    class="bg-[#b39359] text-white px-5 py-2 rounded-lg font-semibold hover:bg-[#9a7d47] transition-colors whitespace-nowrap text-sm">
                Upload Image
            </button>
        </div>
        <p id="galleryUploadMsg" class="text-sm mt-2 hidden"></p>
    </div>

    <!-- Gallery grid -->
    <div id="galleryGrid" class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
        <div id="galleryLoading" class="col-span-full text-center py-6 text-gray-400 text-sm">Loading images...</div>
    </div>
</section>
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    @if (!string.IsNullOrEmpty(projectId))
    {
    <script>
        const projectId = '@projectId';
        const antiForgeryToken = document.querySelector('input[name="__RequestVerificationToken"]')?.value ?? '';

        async function loadGallery() {
            const res = await fetch(`/Admin/GetGalleryImages?projectId=${projectId}`);
            const images = await res.json();
            renderGallery(images);
        }

        function renderGallery(images) {
            const grid = document.getElementById('galleryGrid');
            if (!images || images.length === 0) {
                grid.innerHTML = '<p class="col-span-full text-sm text-gray-400 py-4">No gallery images yet. Upload some above.</p>';
                return;
            }
            grid.innerHTML = images.map(img => `
                <div id="gallery-item-${img.id}" class="relative group rounded-xl overflow-hidden shadow border border-gray-100">
                    <img src="${img.imageUrl}" alt="${img.caption ?? ''}" class="w-full h-40 object-cover" />
                    ${img.caption ? `<p class="text-xs text-gray-600 px-2 py-1 truncate">${img.caption}</p>` : ''}
                    <button onclick="deleteGalleryImage('${img.id}')"
                            class="absolute top-2 right-2 bg-red-600 text-white rounded-full w-7 h-7 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity text-xs font-bold hover:bg-red-700">
                        ✕
                    </button>
                </div>`).join('');
        }

        async function uploadGalleryImage() {
            const fileInput = document.getElementById('galleryFileInput');
            const captionInput = document.getElementById('galleryCaptionInput');
            const btn = document.getElementById('galleryUploadBtn');
            const msg = document.getElementById('galleryUploadMsg');

            if (!fileInput.files || fileInput.files.length === 0) {
                showGalleryMsg('Please select an image file.', false);
                return;
            }

            const formData = new FormData();
            formData.append('projectId', projectId);
            formData.append('imageFile', fileInput.files[0]);
            formData.append('caption', captionInput.value);
            formData.append('__RequestVerificationToken', antiForgeryToken);

            btn.disabled = true;
            btn.textContent = 'Uploading…';
            msg.classList.add('hidden');

            try {
                const res = await fetch('/Admin/AddGalleryImage', { method: 'POST', body: formData });
                const data = await res.json();
                if (data.success) {
                    showGalleryMsg('Image uploaded successfully!', true);
                    fileInput.value = '';
                    captionInput.value = '';
                    loadGallery();
                } else {
                    showGalleryMsg(data.message ?? 'Upload failed.', false);
                }
            } catch {
                showGalleryMsg('An error occurred. Please try again.', false);
            } finally {
                btn.disabled = false;
                btn.textContent = 'Upload Image';
            }
        }

        async function deleteGalleryImage(imageId) {
            if (!confirm('Remove this image from the gallery?')) return;

            const formData = new FormData();
            formData.append('id', imageId);
            formData.append('__RequestVerificationToken', antiForgeryToken);

            try {
                const res = await fetch('/Admin/DeleteGalleryImage', { method: 'POST', body: formData });
                const data = await res.json();
                if (data.success) {
                    document.getElementById(`gallery-item-${imageId}`)?.remove();
                    const grid = document.getElementById('galleryGrid');
                    if (grid.children.length === 0) {
                        grid.innerHTML = '<p class="col-span-full text-sm text-gray-400 py-4">No gallery images yet. Upload some above.</p>';
                    }
                } else {
                    alert(data.message ?? 'Failed to delete image.');
                }
            } catch {
                alert('An error occurred. Please try again.');
            }
        }

        function showGalleryMsg(text, success) {
            const msg = document.getElementById('galleryUploadMsg');
            msg.textContent = text;
            msg.className = `text-sm mt-2 ${success ? 'text-green-600' : 'text-red-600'}`;
        }

        loadGallery();
    </script>
    }
}
